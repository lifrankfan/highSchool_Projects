1.  Edit a file named "main.swift"
2.  Write a function named "readLines" which accepts no parameters and returns an array
    of String.
    The function must read lines of text from Standard Input until end-of-file is reached.
    The function must return an array of all lines read.
4.  Write a function named "printLines" which accepts one parameter named 'lines',
    an array of String.
    The function must print the contents of the array, one element per line.
